name = "vikarin-config-api"
main = "build/shim.js"
compatibility_date = "2024-10-01"
workers_dev = true
account_id = "ad9560f088d539c97b198f7b0e0f0c94"
 # CI will run worker-build prior to wrangler deploy (see workflow).

[observability]
[observability.logs]
enabled = false
head_sampling_rate = 1
invocation_logs = true
persist = true

# KV namespace binding. After running `wrangler kv namespace create CONFIG_KV`,
# set `id` (and `preview_id` if provided) here.
[[kv_namespaces]]
binding = "CONFIG_KV"
# For local `wrangler dev`, no `id` is required. Set this on publish.
id = "8d296667d3af4a7f80d854d0a4b49243"

# For local dev: run `worker-build --release` to create `build/` (shim.js, index.js, wasm).

